# NOTE: keep this file ASCII-only. Java .properties files are read as ISO-8859-1,
# so non-ASCII comments can corrupt line parsing after Unity's template expansion.
#
# TLS note: this PC runs Norton HTTPS inspection (TLS MITM). Maven downloads only
# work because the build uses a patched JDK copy (C:\dev\UnityOpenJDK17) whose
# cacerts contains Norton's root CA. See README and the THETA editor menu item 4.
org.gradle.jvmargs=-Xmx**JVM_HEAP_SIZE**M
org.gradle.parallel=true
unityStreamingAssets=**STREAMING_ASSETS**
# Required for androidx (Media3/ExoPlayer) dependencies
android.useAndroidX=true
**ADDITIONAL_PROPERTIES**
